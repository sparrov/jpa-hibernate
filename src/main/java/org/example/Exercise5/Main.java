package org.example.Exercise5;

public class Main {
    public static void main(String[] args) {
/*        EntityManagerFactory entityManagerFactory = Persistence.createEntityManagerFactory("jpa.hibernate");
        EntityManager entityManager = entityManagerFactory.createEntityManager();

        Author author1 = new Author("Stephen", "King");
        Author author2 = new Author("Tess", "Gerritsen");

        Book book1 = new Book("Lśnienie");
        Book book2 = new Book("Chirurg");

        Chapter chapter1 = new Chapter(1, "Rozmowa z pracodawcą");
        Chapter chapter2 = new Chapter(2, "Boulder");

        book1.setAuthors(List.of(author1));
        book2.setAuthors(List.of(author2));
        //book1.setChapters(List.of(chapter1, chapter2));
        chapter1.setBook(book1);
        chapter2.setBook(book1);


        entityManager.getTransaction().begin();
        entityManager.persist(author1);
        entityManager.persist(author2);
        entityManager.persist(book1);
        entityManager.persist(book2);
        entityManager.persist(chapter1);
        entityManager.persist(chapter2);
        entityManager.getTransaction().commit();

        entityManager.close();
        entityManagerFactory.close();*/
    }
}
